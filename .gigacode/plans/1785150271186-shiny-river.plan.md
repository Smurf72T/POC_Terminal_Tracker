# План реорганизации структуры UI-файлов

## Текущая ситуация

В каталоге `src/ui` находились 36 файлов разных типов:
- 11 форм (mainwindow, manufacturersform, modelsform, clientsform, simcardsform, terminalsform, receiptform, rentalform, returnform, paymentform, archivedocumentsform)
- 4 делегата (comboboxdelegate, readonlydelegate, CheckBoxDelegate, comboboxmodel)
- 1 главный файл приложения (main.cpp)

## Выполненные изменения

Структура успешно реорганизована:

```
src/ui/
├── mainwindow.h/cpp/ui       # Главное окно (остаётся на верхнем уровне)
├── dialogs/                  # Все формы документов (30 файлов)
│   ├── manufacturersform.h/cpp/ui
│   ├── modelsform.h/cpp/ui
│   ├── clientsform.h/cpp/ui
│   ├── simcardsform.h/cpp/ui
│   ├── terminalsform.h/cpp/ui
│   ├── receiptform.h/cpp/ui
│   ├── rentalform.h/cpp/ui
│   ├── returnform.h/cpp/ui
│   ├── paymentform.h/cpp/ui
│   └── archivedocumentsform.h/cpp/ui
└── delegates/                # Кастомные делегаты (4 файла)
    ├── comboboxdelegate.h
    ├── readonlydelegate.h
    ├── CheckBoxDelegate.h
    └── comboboxmodel.h
```

## Зависимости и изменения в include-путях

### Обновленные файлы:

1. **mainwindow.cpp** - обновлены 10 путей:
   - `"manufacturersform.h"` → `"dialogs/manufacturersform.h"`
   - `"modelsform.h"` → `"dialogs/modelsform.h"`
   - `"clientsform.h"` → `"dialogs/clientsform.h"`
   - `"simcardsform.h"` → `"dialogs/simcardsform.h"`
   - `"terminalsform.h"` → `"dialogs/terminalsform.h"`
   - `"receiptform.h"` → `"dialogs/receiptform.h"`
   - `"rentalform.h"` → `"dialogs/rentalform.h"`
   - `"returnform.h"` → `"dialogs/returnform.h"`
   - `"paymentform.h"` → `"dialogs/paymentform.h"`
   - `"archivedocumentsform.h"` → `"dialogs/archivedocumentsform.h"`

2. **rentalform.cpp** (старый и в dialogs/) - обновлены:
   - `"comboboxdelegate.h"` → `"../ui/delegates/comboboxdelegate.h"`
   - `"comboboxmodel.h"` → `"../ui/delegates/comboboxmodel.h"`

3. **receiptform.cpp** (в dialogs/) - обновлен:
   - `"comboboxdelegate.h"` → `"../ui/delegates/comboboxdelegate.h"`

4. **simcardsform.cpp** и **simcardsform.h** (старые и в dialogs/) - обновлены:
   - `"readonlydelegate.h"` → `"../ui/delegates/readonlydelegate.h"`

5. **archivedocumentsform.cpp** (старый и в dialogs/) - обновлен:
   - `"CheckBoxDelegate.h"` → `"../ui/delegates/CheckBoxDelegate.h"`

## Измененные файлы

### 1. CMakeLists.txt
Обновлены пути в переменных `SOURCES`, `HEADERS`, `UI_FILES`:
- Все формы перемещены в `src/ui/dialogs/`
- Все делегаты перемещены в `src/ui/delegates/`

### 2. main.cpp
Без изменений (`#include "ui/mainwindow.h"`)

## Выполненные шаги

1. ✅ Созданы новые подкаталоги: `src/ui/dialogs/`, `src/ui/delegates/`
2. ✅ Скопированы все файлы форм в `src/ui/dialogs/`
3. ✅ Скопированы все файлы делегатов в `src/ui/delegates/`
4. ✅ Обновлен `CMakeLists.txt` с новыми путями
5. ✅ Обновлены `#include`-директивы во всех `.cpp` и `.h` файлах
6. ✅ Удалены старые файлы из `src/ui/` (остались только mainwindow.* и каталоги)

## Результат

После выполнения реорганизации:
- `src/ui/` содержит только `mainwindow.*` и подкаталоги `dialogs/`, `delegates/`
- Все формы сгруппированы в `src/ui/dialogs/`
- Все делегаты сгруппированы в `src/ui/delegates/`
- CMakeLists.txt и все исходники обновлены

**Примечание:** CMake не установлен в PATH системы. Для сборки проекта используйте:
```bash
cd C:\Users\Smurf\Desktop\POC_Terminal_Tracker
cmake -B cmake-build-debug
cmake --build cmake-build-debug
```

Или откройте проект в Qt Creator для сборки.
