# Аудит проекта POC Terminal Tracker — готовность к продакшену

**Дата:** 03.08.2026
**Версия:** 1.5.2
**Статус проверки:** замечания проанализированы и приняты к исполнению (см. `audit.md`, раздел 9, этап 6 и `CHANGELOG.md` 1.5.3). Устаревшие/ложные пункты (4, 5, 9, 11, 12) сняты; реализованы: HTTPS для обновлений, sslmode=require, CMake-проверка утилит, предупреждение о пустом пароле, тесты `OpsScheduler`/`UpdateManager`, исправлен queued-баг `BackupResult`.

---

## 1. Архитектура и код

### ✅ Сильные стороны
- **Многопользовательская конкурентность** — блокировки `FOR UPDATE NOWAIT`, advisory locks для миграций, атомарные UPDATE для rate limiting. Тесты `test_concurrency` это подтверждают.
- **Circuit Breaker** — защита от каскадных сбоев БД, состояниями Closed/Open/HalfOpen.
- **Connection Pooling** — per-thread пул соединений для фоновых операций (BackupWorker).
- **Миграции БД** — транзакционные, с advisory lock от гонок, идемпотентные.
- **Фоновый бэкап** — `BackupWorker` в отдельном потоке, не блокирует UI.
- **Шифрование бэкапов** — AES-256-CBC с маркером `POCENC1`, обратная совместимость.
- **Health-check CLI** — `--check-db`, `--version`, `--help`.
- **Graceful shutdown** — `aboutToQuit` закрывает БД.
- **Certificate pinning** — SPKI SHA-256 для автообновлений.
- **SHA256-верификация** обновлений при скачивании.
- **Пагинация** в `TerminalsForm` (LIMIT/OFFSET).
- **Интерфейс `IDatabaseManager`** — корректная подмена в тестах (без moc-хаков).

### ⚠️ Выявленные проблемы

#### Критические (P0)

| # | Проблема | Где | Описание |
|---|----------|-----|----------|
| 1 | **Хардкод пароля admin в миграции 001** | `001_initial.sql` | Пароль `Admin123` хранится в SQL-скрипте. Миграция 009 пытается это исправить (`must_change_password`), но если миграция не применится — пароль остаётся известным. |
| 2 | **Нет ротации логов OpsLog** | `opslog.cpp` | В заголовке заявлена ротация при 1 МБ, но нужно проверить реализацию. |
| 3 | **`DatabaseManager` — singleton + static** | `databasemanager.h` | Глобальное состояние singleton усложняет тестирование и не thread-safe при инициализации (хотя `static local` в C++11 гарантирует thread-safe, но `setSuppressDialogs` — статическое поле без синхронизации). |

#### Важные (P1)

| # | Проблема | Где | Описание |
|---|----------|-----|----------|
| 4 | **Отсутствует `docs/API.md` и `docs/adr/`** | `docs/` | В CHANGELOG заявлены эти файлы, но они не видны в структуре проекта. Нужно проверить их наличие. |
| 5 | **QXlsx — внешний submodule** | `libs/QXlsx/` | Подключен через `add_subdirectory`. Версия не зафиксирована в git tag. Риск: submodule может быть изменён. |
| 6 | **Нет проверки `pg_dump`/`psql`/`openssl` при конфигурации CMake** | `CMakeLists.txt` | Fallback-дамп работает без внешних утилит, но в продакшене без `pg_dump` бэкап будет медленным и неполным. |
| 7 | **`config.json` — дефолтный пароль пустой** | `config/config.json` | `"password": ""` — если `.env` не настроен, подключение может не работать или работать с пустым паролем. |
| 8 | **Нет `sslrootcert` по умолчанию** | `config.json` | `sslmode: prefer` без проверки сертификата — уязвимость MITM. |
| 9 | **`BackupManager::createFallbackBackup` — потенциальная утечка памяти** | `backupmanager.cpp` | `QTemporaryDir tmpDir` в `createBackup` и `restoreDatabase` — если произойдёт исключение или ранний return до `return result`, временая директория может не очиститься. |
| 10 | **Нет валидации входных данных на уровне БД** | SQL-схема | Многие поля (имена клиентов, комментарии) имеют `VARCHAR(150)`/`TEXT` без ограничений длины, что может привести к проблемам с производительностью. |

#### Средние (P2)

| # | Проблема | Где | Описание |
|---|----------|-----|----------|
| 11 | **Нет интеграционных тестов для `BackupManager`** | `tests/` | Тесты есть для connection pool, circuit breaker, validator, password utils, но нет тестов на реальный бэкап/восстановление. |
| 12 | **`OpsLog::rotateIfNeeded` — нет проверки реализации** | `opslog.cpp` | Нужно убедиться, что ротация работает корректно. |
| 13 | **Нет тестов для `UpdateManager`** | `tests/` | `test_update_utils` тестирует только `version.h`, но не сетевую логику. |
| 14 | **Нет тестов для `OpsScheduler`** | `tests/` | Планировщик автобэкапов не покрыт тестами. |
| 15 | **`MainWindow` — монолитный** | `mainwindow.cpp` | Конструктор ~300 строк, множество connect-блоков. Сложно тестировать и поддерживать. |
| 16 | **Нет `CMakePresets.json`** | корень | Для удобства разработчиков (VS Code, CLion). |
| 17 | **Нет `.editorconfig`** | корень | Единый стиль форматирования для IDE. |
| 18 | **Нет лицензионного файла проекта** | корень | QXlsx имеет лицензию, но сам проект — нет. |
| 19 | **`resources.qrc` — нужно проверить** | корень | Нужно убедиться, что все стили и медиа включены корректно. |
| 20 | **Нет мониторинга/алертинга** | архитектура | Нет механизмов оповещения об ошибках в продакшене (кроме логов). |

#### Низкие (P3)

| # | Проблема | Где | Описание |
|---|----------|-----|----------|
| 21 | **Нет `CHANGELOG.md` для версий до 1.0.0** | `CHANGELOG.md` | История начинается с 1.0.0, но проект развивался раньше. |
| 22 | **Нет semantic versioning тегов в git** | git | Для автоматизации релизов. |
| 23 | **Нет `CODE_OF_CONDUCT.md`** | корень | Если проект планируется open-source. |
| 24 | **`windeployqt` — только Windows** | CI | Linux CI не собирает дистрибутив (только тесты). |
| 25 | **Нет Dockerfile** | корень | Для контейнеризации БД и тестов. |

---

## 2. Безопасность

### ✅ Хорошее
- PBKDF2-HMAC-SHA256 с 100k итераций
- Constant-time сравнение хешей
- Rate limiting на уровне БД
- SQL-инъекции устранены (форматирование через `QSqlField::formatValue`)
- SSL с проверкой сертификата (при настройке)
- Certificate pinning для обновлений
- AES-256-CBC шифрование бэкапов
- Ролевая защита на уровне БД (триггеры)
- Аудит действий пользователей

### ⚠️ Требует внимания
- **Хардкод пароля в 001_initial.sql** — критично для первой установки
- **`sslmode: prefer` по умолчанию** — без проверки сертификата уязвим к MITM
- **Нет HTTPS для автообновлений** — `update.url` должен быть HTTPS, но это не проверяется
- **Пароли бэкапов** — передаются как аргументы/env var, могут быть видны в `/proc`
- **Нет двухфакторной аутентификации** — для admin-учёток рекомендуется

---

## 3. Тестирование

| Категория | Покрытость | Комментарий |
|-----------|-----------|-------------|
| Unit-тесты | ~60% | Password utils, validator, version, connection pool, circuit breaker — хорошо. Нет тестов для OpsScheduler, UpdateManager (сетевая часть), BackupManager. |
| Интеграционные | ~40% | `test_db_integration` и `test_concurrency` против реального PostgreSQL. Нет тестов бэкапа/восстановления. |
| Fuzz-тесты | ~10% | Только `test_validator` (fuzz_random_validator_inputs, fuzz_password_hashing). |
| UI-тесты | ~5% | 5 тестов для делегатов. Нет тестов основных форм. |

---

## 4. CI/CD

### ✅ Хорошее
- GitHub Actions: Windows (MSVC) + Linux (gcc)
- Установка PostgreSQL в раннер
- Прогон всех тестов (включая DB integration и concurrency)
- Сборка дистрибутива (ZIP + NSIS)
- Code signing (опционально, через секреты)
- Code coverage (gcovr + Codecov)
- Auto-bump версии при push в master

### ⚠️ Требует внимания
- **Linux CI** не собирает дистрибутив (только тесты)
- **Нет тестирования на macOS** (если планируется поддержка)
- **Нет автоматических релизов** (GitHub Releases)
- **Нет проверки лицензий зависимостей**

---

## 5. Документация

### ✅ Есть
- `README.md`
- `CHANGELOG.md` (детальный, с датами)
- `docs/OPS.md`
- `.env.example`

### ⚠️ Недостатки
- **Нет `docs/API.md`** (заявлен в CHANGELOG, но не найден)
- **Нет `docs/adr/`** (заявлен в CHANGELOG, но не найден)
- **Нет инструкций по развёртыванию в продакшене** (кроме OPS.md)
- **Нет документации по конфигурации** (что означает каждый параметр в config.json)
- **Нет diagrams** (архитектура, поток данных)

---

## 6. Конфигурация и развёртывание

| Аспект | Статус |
|--------|--------|
| Конфигурационный файл | ✅ JSON, с примером |
| Переменные окружения | ✅ `.env` поддержка |
| Портативная сборка | ✅ ZIP + NSIS |
| Автообновление | ✅ С верификацией |
| Бэкап/восстановление | ✅ С шифрованием |
| SSL/TLS | ⚠️ Настройка по умолчанию не безопасна |
| Инициализация БД | ✅ Миграции |

---

## 7. Итоговая оценка

| Категория | Оценка | Комментарий |
|-----------|--------|-------------|
| Функциональность | **9/10** | Полный набор бизнес-функций |
| Безопасность | **7/10** | Хорошая база, но хардкод пароля и SSL prefer — риски |
| Надёжность | **8/10** | Retry, circuit breaker, graceful shutdown, concurrency tests |
| Тестируемость | **6/10** | Хорошие unit-тесты, но мало coverage для ops/update/UI |
| Документация | **5/10** | CHANGELOG отличный, но API/ADR не найдены, нет deployment guide |
| CI/CD | **8/10** | Хороший конвейер, но нет macOS и auto-releases |
| Архитектура | **8/10** | Чистая, но MainWindow — монолит |
| **Общая готовность** | **7/10** | **Требует доработки перед продакшеном** |

---

## 8. Рекомендации (приоритетно)

1. **P0**: Заменить хардкод пароля в `001_initial.sql` на случайный генерируемый при первой установке
2. **P0**: Проверить реализацию ротации логов в `opslog.cpp`
3. **P1**: Добавить `docs/API.md` и `docs/adr/` (или убрать из CHANGELOG)
4. **P1**: Изменить `sslmode` по умолчанию на `require` или `verify-ca`
5. **P1**: Добавить проверку HTTPS для `update.url`
6. **P1**: Добавить CMake-проверку наличия `pg_dump`/`psql`/`openssl`
7. **P2**: Написать тесты для `BackupManager` (backup + restore roundtrip)
8. **P2**: Написать тесты для `OpsScheduler`
9. **P2**: Добавить `.editorconfig`, `CMakePresets.json`, лицензионный файл
10. **P3**: Добавить Dockerfile для PostgreSQL в тесты
11. **P3**: Настроить автоматические GitHub Releases
